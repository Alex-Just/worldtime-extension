import type { CSSProperties } from 'react';

export const styles: { [key: string]: CSSProperties } = {
  timeText: {
    fontSize: '34px',
    fontWeight: '100',
    color: '#a5a5a5',
    marginBottom: '3px',
    marginLeft: '-5px',
  },
};
